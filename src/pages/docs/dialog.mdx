import { MDXLayoutNext } from '@/components';

export const meta = {
  title: 'Dialog',
  description: 'Dialog component | Fluid Design',
  date: '2023-02-01',
  author: 'Oliver Pan',
  tags: [
    'fluid ui',
    'react',
    'framer motion',
    'headless ui',
    'tailwindcss',
    'dialog',
    'modal',
  ],
};

export default (props) => (
  <MDXLayoutNext meta={meta} components={{}} {...props} />
);

# Dialog

Dialog (Modal) is used to display content that requires user interaction. They are used to display information, ask for input, or require a decision.

Examples coming soon.
