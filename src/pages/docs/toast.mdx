import { MDXLayoutNext } from '@/components';

export const meta = {
  title: 'Toast',
  description: 'Toast component | Fluid Design',
  date: '2023-02-01',
  author: 'Oliver Pan',
  tags: [
    'fluid ui',
    'react',
    'framer motion',
    'headless ui',
    'tailwindcss',
    'toast',
  ],
};

export default (props) => (
  <MDXLayoutNext meta={meta} components={{}} {...props} />
);

# Toast

Toast is used to display a message to the user. They are usually displayed at the top of the screen and disappear after a few seconds.

Examples coming soon.
