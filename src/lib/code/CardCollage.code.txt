/* 
V1.0.0
Card Collage
Component by fluid-design.io
Last updated: 1/12/21
*/
//@ts-ignore
import { ImageCollageComponent } from "../../image";

function Example() {
  const tags = [
    {
      id: 1,
      name: "Nature",
    },
    {
      id: 2,
      name: "Wallpaper",
    },
    {
      id: 3,
      name: "Fog",
    },
  ];
  return (
    <>
      <ImageCollageComponent />
      <div className="w-full max-w-xs dark:text-white">
        <div className="pt-4 false">
          <h3 className="flex items-center font-bold text-left capitalize ">
            Forest
          </h3>
        </div>
        <p className="pt-1 text-sm text-primary-600 dark:text-primary-400 prefers-contrast:text-primary-800 dark:prefers-contrast:text-primary-100 prefers-contrast:font-medium">
          32 photos •{" "}
          <span className="cursor-pointer hover:opacity-80 hover:underline">
            <a href="" aria-label={`Browse more images by John Doe`}>
              By John Doe
            </a>
          </span>
        </p>
        <div className="-mt-1">
          <ul className="flex flex-wrap justify-start w-full pt-4 gap-x-3 no-scroll">
            {tags.map(({ id, name }) => (
              <li key={id} className="list-none">
                <button
                  className="px-4 py-1 text-sm font-medium capitalize rounded-full focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 bg-primary-200 dark:bg-primary-700 prefers-contrast:hover:ring-2 prefers-contrast:hover:ring-primary-800 dark:prefers-contrast:hover:ring-primary-50 prefers-contrast:hover:ring-offset-2 hover:bg-primary-300 dark:hover:bg-primary-600 prefers-contrast:font-semibold prefers-contrast:border prefers-contrast:border-primary-700 dark:prefers-contrast:border-primary-200 dark:prefers-contrast:bg-primary-800 dark:prefers-contrast:text-primary-50 prefers-contrast:text-primary-800"
                  aria-label={`Browse category related to ${name}`}
                  onClick={() => null}
                >
                  <span className="sr-only">{name}</span>
                  {name}
                </button>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </>
  );
}
export default Example;
